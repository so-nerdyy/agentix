name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  validate:
    name: Build, test, and release smoke (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Build packaged terminal UI
        run: npm run build:tui

      - name: Terminal UI type-check
        run: npm run type-check --prefix hermes-agent/ui-tui

      - name: Terminal UI tests
        timeout-minutes: 10
        run: npm test --prefix hermes-agent/ui-tui

      - name: Unit tests
        run: npm test

      - name: Python backend adapter tests
        run: python -m unittest tests/python/test_agentix_backend_adapter.py

      - name: Release smoke
        timeout-minutes: 25
        run: npm run smoke:release

      - name: Shell installer dry run
        timeout-minutes: 5
        if: runner.os != 'Windows'
        run: AGENTIX_DRY_RUN=1 AGENTIX_SKIP_SETUP=1 sh install.sh

      - name: PowerShell installer dry run
        timeout-minutes: 5
        if: runner.os == 'Windows'
        shell: pwsh
        run: .\install.ps1 -DryRun -SkipSetup

  docker:
    name: Docker validation
    runs-on: ubuntu-latest
    needs: validate

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Build Agentix runtime image
        run: docker build -t agentix:ci .

      - name: Validate Agentix compose
        run: docker compose -f docker-compose.yml config
        env:
          AGENTIX_SESSION_TOKEN: ci-token

      - name: Build Agentix compatibility frontend image
        run: docker build -f hermes-agent/Dockerfile hermes-agent

      - name: Validate compatibility Linux compose
        run: docker compose -f hermes-agent/docker-compose.yml config

      - name: Validate compatibility Windows compose
        run: docker compose -f hermes-agent/docker-compose.windows.yml config
